# Изменения в систему распознавания и валидации товаров

**Дата**: 14 декабря 2025  
**Автор**: GitHub Copilot  
**Цель**: Показывать пользователю распознанный текст в том виде, в котором он будет отправлен в СБИС

## 📋 Проблема

До изменений:
1. После распознавания текста пользователю показывались **сырые названия из OCR**
2. Валидация и сопоставление с каталогом происходили **только при отправке** в СБИС
3. Иногда в СБИС попадали **не те позиции**, потому что названия преобразовывались уже после подтверждения

## ✅ Решение

Теперь валидация и нормализация происходят **сразу после распознавания**, и пользователь видит:
- ✅ Исходное название из OCR
- ✅ Нормализованное название из каталога (если отличается)
- ✅ Именно то, что будет отправлено в СБИС

## 🔧 Технические изменения

### 1. Новая функция `validate_and_normalize_items()`

**Расположение**: `bot_simple.py`, строка ~230

**Что делает**:
- Принимает сырые позиции из OCR
- Для каждого товара ищет совпадение в каталоге/составах
- Возвращает список с:
  - `name` — исходное название из OCR
  - `qty` — количество
  - `catalog_name` — нормализованное название из каталога

**Пример работы**:
```python
# Вход (из OCR)
items = [
    {"name": "тесто", "qty": 2.5},
    {"name": "песто", "qty": 1.0}
]

# Выход (после валидации)
validated = [
    {
        "name": "тесто",
        "qty": 2.5,
        "catalog_name": "ТЕСТО СЛОЕНОЕ ДЛЯ ПИЦЦЫ"
    },
    {
        "name": "песто",
        "qty": 1.0,
        "catalog_name": "СОУС ПЕСТО ЗЕЛЁНЫЙ"
    }
]
```

### 2. Обновлённая функция `format_items()`

**Расположение**: `bot_simple.py`, строка ~134

**Что изменилось**:
- Теперь показывает преобразование: `тесто → ТЕСТО СЛОЕНОЕ ДЛЯ ПИЦЦЫ — 2.5`
- Если названия совпадают, показывает просто: `СОУС ПЕСТО ЗЕЛЁНЫЙ — 1.0`

### 3. Обновлённая функция `handle_photo()`

**Расположение**: `bot_simple.py`, строка ~667

**Что изменилось**:
```python
# СТАРАЯ ЛОГИКА:
# 1. OCR распознавание
# 2. Показываем сырой список
# 3. Ждём подтверждения
# 4. При "да" - валидация и отправка

# НОВАЯ ЛОГИКА:
# 1. OCR распознавание
# 2. Валидация и нормализация
# 3. Показываем нормализованный список (что уйдет в СБИС)
# 4. Ждём подтверждения
# 5. При "да" - отправка (без повторной валидации)
```

**Новое сообщение пользователю**:
```
Тип документа: Производство
Вот что будет отправлено в СБИС:
1. тесто → ТЕСТО СЛОЕНОЕ ДЛЯ ПИЦЦЫ — 2.5
2. песто → СОУС ПЕСТО ЗЕЛЁНЫЙ — 1.0
3. крутоны → СУХАРИКИ ЧЕСНОЧНЫЕ — 0.5

Все верно?
```

### 4. Обновлённая функция `send_act_by_type()`

**Расположение**: `bot_simple.py`, строка ~347

**Что изменилось**:
- Теперь использует `catalog_name` вместо `name` при отправке
- Не нужна повторная валидация - товары уже проверены

```python
# СТАРОЕ:
# items = [{"name": "тесто", "qty": 2.5}]
# → Валидация при отправке → Может возникнуть ошибка

# НОВОЕ:
# items = [{"name": "тесто", "catalog_name": "ТЕСТО СЛОЕНОЕ", "qty": 2.5}]
# → Используем catalog_name → Всегда корректно
```

### 5. Валидация при редактировании через GPT

**Расположение**: `bot_simple.py`, строка ~593

**Что изменилось**:
- После правки через GPT (`correct_items_with_instruction`) также вызывается валидация
- Пользователь видит нормализованный список после правок

## 🎯 Результаты

### До изменений:
```
Бот: Нашёл такие позиции:
1. тесто — 2.5
2. песто — 1.0

Пользователь: да

Бот: Отправляю...
[Внутри происходит валидация]
[Может возникнуть ошибка "товар не найден"]
[Или в СБИС уходит не то название]
```

### После изменений:
```
Бот: Обрабатываю таблицу на фото через GPT...
Бот: Проверяю товары по каталогу...

Бот: Тип документа: Производство
Вот что будет отправлено в СБИС:
1. тесто → ТЕСТО СЛОЕНОЕ ДЛЯ ПИЦЦЫ — 2.5
2. песто → СОУС ПЕСТО ЗЕЛЁНЫЙ — 1.0

Все верно?

Пользователь: да

Бот: Отправляю...
[Отправка без дополнительной валидации]
Бот: Акт отправлен в СБИС ✅
```

## 💡 Преимущества

1. **Прозрачность**: Пользователь видит точно то, что уйдёт в СБИС
2. **Предсказуемость**: Нет сюрпризов при отправке
3. **Меньше ошибок**: Валидация происходит один раз, сразу после распознавания
4. **Лучший UX**: Пользователь может исправить ошибки до отправки

## 📝 Совместимость

- ✅ Работает со всеми типами документов: Производство, Списание, Приход
- ✅ Сохранена обратная совместимость с существующим кодом
- ✅ Все существующие команды работают как прежде
- ✅ Голосовой ввод и правки через GPT также используют валидацию

## 🔍 Тестирование

Рекомендуется протестировать:
1. ✅ Распознавание фото с разными типами документов
2. ✅ Правку через текст после распознавания
3. ✅ Голосовой ввод для правки
4. ✅ Ручной ввод позиций текстом
5. ✅ Отправку в СБИС с разными типами товаров

## 📂 Затронутые файлы

- `bot_simple.py` - основные изменения
- `test_validation.py` - новый файл для тестирования (опционально)

## 🚀 Развёртывание

После обновления кода на сервере:

```bash
cd ~/bot_app
git pull
sudo systemctl restart sbis_bot
```

Проверить статус:
```bash
sudo systemctl status sbis_bot
journalctl -u sbis_bot -f
```
