# Быстрое руководство по устранению ошибок распознавания

## Что было исправлено

### ✅ Проблема 1: Перемешивание ячеек в больших таблицах
**Было**: GPT читал таблицу неправильно, брал количество из одной строки, а название из другой.

**Исправлено**: 
- Переписан промпт с четкими инструкциями читать построчно
- Добавлена проверка соответствия названия и количества в одной строке

### ✅ Проблема 2: Неправильное сопоставление с каталогом
**Было**: Голосовой ввод распознавал "тесто", но система не могла найти в каталоге или находила неправильный товар.

**Исправлено**:
- Добавлена обработка опечаток (пропущенные буквы, лишние пробелы)
- Добавлена замена латинских букв на кириллические (o→о, a→а, e→е и т.д.)
- Улучшен алгоритм поиска похожих названий
- Добавлено логирование для отладки

### ✅ Проблема 3: Невозможность отследить ошибки
**Было**: Не было логов, непонятно где именно происходит ошибка.

**Исправлено**:
- Все результаты OCR логируются в `logs/ocr_tables.log`
- Все поиски по каталогу логируются в `logs/catalog_matching.log`
- Предупреждения при неточных совпадениях

## Как теперь пользоваться

### 📸 Фотографирование таблиц

**Важно**:
1. Хорошее освещение (без теней)
2. Камера параллельно листу (не под углом)
3. Заголовок документа ("Производство", "Списание", "Приход") должен быть виден
4. Вся таблица в кадре, текст читаемый

### 🎤 Голосовой ввод

Теперь работает лучше! Можно:
- Добавлять позиции: "тесто пять, крутоны два"
- Править количество: "тесто не два, а три"
- Менять название: "измени песто на тесто"
- Удалять: "убери крутоны"

### 📝 Текстовый ввод

Формат: `НАЗВАНИЕ КОЛИЧЕСТВО`

Примеры:
```
тесто 5
крутоны 2, капуста 3
```

### ✅ Проверка перед отправкой

После распознавания бот спросит: "Все верно?"

- Ответ "да" / "ок" / "верно" → отправит акт
- Любой другой текст → воспримется как команда правки

## Что делать если ошибки продолжаются

### 1. Проверьте логи

```bash
# Смотрим последние OCR результаты
tail -50 logs/ocr_tables.log

# Смотрим проблемные сопоставления (score < 0.7)
grep -E "score: 0\.[5-6]" logs/catalog_matching.log
```

### 2. Запустите тест

```bash
python3 test_ocr_diagnosis.py
```

Это покажет:
- Есть ли сохраненные изображения для анализа
- Как работает сопоставление с каталогом
- Какие товары находит в каталоге

### 3. Частые проблемы и решения

#### Проблема: "Товар не найден в Каталоге"
**Решение**:
1. Проверьте, есть ли товар в файле `Каталог.xlsx`
2. Посмотрите в логах, какие похожие товары нашлись
3. Используйте точное название из каталога
4. Попробуйте голосовой ввод вместо фото

#### Проблема: "Не нашел ни одной строки с количеством на фото"
**Решение**:
1. Переснимите фото при лучшем освещении
2. Убедитесь, что количества написаны четко
3. Проверьте, что таблица не слишком большая (не больше 20-30 строк)
4. Введите позиции вручную текстом или голосом

#### Проблема: "Перепутал название и количество"
**Решение**:
1. Используйте команду правки: "тесто не 5, а 3"
2. Или очистите список (`/clear`) и введите заново
3. Проверьте в логах `ocr_tables.log`, что именно распознал GPT

### 4. Команды для отладки

```
/list   - показать текущий список позиций
/clear  - очистить список и начать заново
/send номер дата - отправить вручную
```

## Советы для лучшей работы

### ✅ Делайте так:
- Фотографируйте при дневном свете или ярком искусственном освещении
- Используйте голосовой ввод для небольших списков (2-5 позиций)
- Проверяйте список командой `/list` перед отправкой
- Пишите названия так же, как в каталоге (регистр не важен)

### ❌ Избегайте:
- Фото в тени или при плохом свете
- Фото под углом (держите параллельно)
- Очень большие таблицы на одном фото (лучше разбить)
- Рукописные количества неразборчивым почерком

## Примеры работы

### Пример 1: Фото таблицы
1. Сфотографировали лист "Производство" с таблицей
2. Отправили фото боту
3. Бот показал: "Тип документа: Производство. Нашел такие позиции: ..."
4. Проверили список
5. Ответили "да" → акт отправлен

### Пример 2: Голосовой ввод
1. Отправили голосовое: "производство"
2. Отправили голосовое: "тесто пять, крутоны два"
3. Написали: "отправить"
4. Бот отправил акт производства

### Пример 3: Правка после фото
1. Отправили фото
2. Бот распознал, но перепутал количество
3. Написали: "тесто не 2, а 5"
4. Бот исправил и снова показал список
5. Ответили "да" → акт отправлен

## Обновленные файлы

Изменения внесены в следующие файлы:
- `ocr_gpt.py` - улучшен промпт и добавлено логирование
- `name_matching.py` - улучшен алгоритм сопоставления названий
- `catalog_lookup.py` - добавлено логирование и обработка ошибок
- `test_ocr_diagnosis.py` - новый скрипт для диагностики (создан)
- `IMPROVEMENTS_REPORT.md` - подробный отчет (создан)

## Вопросы?

Если проблемы остались:
1. Сохраните проблемное фото
2. Скопируйте логи из папки `logs/`
3. Опишите, что ожидали и что получилось
